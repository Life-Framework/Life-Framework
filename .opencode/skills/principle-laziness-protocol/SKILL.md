---
name: principle-laziness-protocol
description: "Use when sizing a change or tempted to add abstraction. Prefer deletion, reuse, inheritance, and the smallest sufficient implementation."
---
# Laziness Protocol
Solve the current problem, not speculative ones. Delete dead code first, reuse existing classes/components, and count callers before adding an abstraction. Prefer a prefab inheritance delta over a copy.
